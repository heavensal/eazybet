<li class="flex-none w-[calc(100%-1rem)] snap-start max-w-[500px] bg-[#0f3460] rounded-lg shadow-[0_0_5px_rgba(0,0,0,0.3)] mb-4 overflow-hidden transition-all duration-300 ease-[cubic-bezier(0.4,_0,_0.2,_1)] border border-[rgba(255,_255,_255,_0.1)] ">

  <div class="p-2.5 h-[30px] flex justify-between bg-[var(--bg-card-dark)] rounded-t-lg items-center">
    <p><%= competition.title %></p>

    <p class="text-white">
      <%= l(commence_time) %>
    </p>
  </div>

  <%= turbo_frame_tag "event_odds_#{event.id}", src: odds_event_path(event), class: "py-4 flex justify-between items-stretch border-b border-[#e5e7eb]" do %>
    <p>
      Chargement des cotes...
    </p>
  <% end %>


    <%= link_to event_path(event), class: "w-full p-4 flex items-center h-[50px]", data: { turbo: false }  do %>
      💬<%= comments.size %>
    <% end %>


</li>
